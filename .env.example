# Copy to .env before: docker compose up -d --build
# cp .env.example .env

HTTP_PUBLISH_PORT=80

# External API used by the SPA (hosted separately)
VITE_API_BASE_URL=https://mafuta.mysafari.co.tz/api
