# Copy to .env and adjust before: docker compose up -d --build
# cp .env.example .env

# Published host ports
HTTP_PUBLISH_PORT=80
API_PUBLISH_PORT=3060
DB_PUBLISH_PORT=5432

# PostgreSQL (must match backend DB_* vars below)
POSTGRES_USER=dev
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=reli_db
